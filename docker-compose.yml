version: "3.9"
services:
    app:
        build: .
        restart: unless-stopped
        ports:
            - "8081:8081"
        environment:
            CONFIG_PATH: ${CONFIG_PATH}
            HTTP_SERVER_PASSWORD: ${HTTP_SERVER_PASSWORD}
